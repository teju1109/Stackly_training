Constructor : constructor is a special method used to initialize 
              an object when it is created.

-->A constructor has the same name as the class.
-->It does not have a return type (not even void).
-->It is called automatically when you create an object using the new keyword.
-->For each constructor we have to create a seperate object

Types of constructors:

1.Default constructor
-->No parameters
Student() {
    name = "Unknown";
}

2.Parameterized constructor
-->Takes arguments to initialize values
Student(String n) {
    name = n;
}

this : a keyword used to refer the current object.

example code of a constructor :

class Student {
    String name;
    int age;
    Student() {
        name = "Teju";
        age = 21;
    }
}

public class Main {
    public static void main(String[] args) {

        Student s1 = new Student();
        System.out.println("Name: " + s1.name);
        System.out.println("Age: " + s1.age);
    }
}

working of the above code :

Step 1: Start with Main Class (Entry Point)
public class Main {
    public static void main(String[] args) {

        Student s1 = new Student();  // object creation

        System.out.println("Name: " + s1.name);
        System.out.println("Age: " + s1.age);
    }
}

Step 2: Now Add the Student Class
class Student {
    String name;
    int age;
}

Step 3: Now Add Constructor (Important Part)
class Student {
    String name;
    int age;

    // Constructor
    Student() {
        name = "Teju";
        age = 21;
    }
}

Steps to Create a Constructor
Step 1: Create a Class
        class Student {}
Step 2: Add Variables (Data)
        class Student 
        {
          String name;
          int age;
        }
Step 3: Write the Constructor
        class Student 
        {
          String name;
          int age;
          Student()
           {   // constructor
             name = "Teju";
             age = 21;
           }
        }
-->Same name as class
-->No return type
-->Used to assign values

final-->Once assigned, you cannot change it.
public class Main {
    public static void main(String[] args) {
        final int x = 10;
        // x = 20; ❌ ERROR (cannot change)
        System.out.println(x);
    }
}

finally-->This block will run no matter what (exception or not)
public class Main {
    public static void main(String[] args) {
        try {
            int a = 10 / 0;  // error
            } 
        catch (Exception e) {
             System.out.println("Exception caught");
            } 
        finally {
            System.out.println("Finally block always runs");
        }
    }
}

finalize()-->Method → called before object is destroyed.
class Demo {
    protected void finalize() {
        System.out.println("Object is destroyed");
    }
}

public class Main {
    public static void main(String[] args) {
        Demo d1 = new Demo();
        d1 = null;   // making object eligible for garbage collection
        System.gc(); // request JVM to clean memory
    }
}

Comparison :
| Feature   | final                   | finally             | finalize()          |
| --------- | ----------------------- | ------------------- | ------------------- |
| Type      | Keyword                 | Block               | Method              |
| Purpose   | Restrict changes        | Always execute code | Cleanup before GC   |
| Used with | Variable, method, class | try-catch           | Object (GC process) |

final stops changes, 
finally always runs and
finalize() is for cleanup before an object is removed.

Garbage Collection (GC) :
It is a process where Java automatically removes unused objects from memory.

example code : 
class Demo {
    public void show() {
        System.out.println("Object is alive");
    }
}

public class Main {
    public static void main(String[] args) {
        Demo d1 = new Demo();  // object created
        d1.show();
        d1 = null;  // object is now unused (eligible for GC)
        System.gc();  // request JVM to run garbage collection
    }
}

-->Java handles memory automatically (no need to free manually)
-->System.gc() is only a request, not a guarantee
-->You don’t control exactly when GC runs





Tasks
1. write a code using default constructor, parameterized constructor and use "this" keyword , all in a single program.

class Student {

    String name;
    int age;
    Student() {
        name = "Tejesh";
        age = 22;
    }
    Student(String name, int age) {
        this.name = name;  
        this.age = age;
    }
    void display() {
        System.out.println("Name: " + name + ", Age: " + age);
    }
}

public class Main {
    public static void main(String[] args) {
        Student s1 = new Student();
        s1.display();
        Student s2 = new Student("Teju", 21);
        s2.display();
    }
}

Step 1: Start with Main 
public class Main {
    public static void main(String[] args) {
        Student s1 = new Student();
        s1.display();
    }
}

Step 2: Add Basic Student Class
class Student {
    String name;
    int age;
    void display() {
        System.out.println("Name: " + name + ", Age: " + age);
    }
}

Step 3: Add Default Constructor
class Student {
    String name;
    int age;

    Student() {
        name = "Unknown";
        age = 0;
    }

    void display() {
        System.out.println("Name: " + name + ", Age: " + age);
    }
}

Step 4: Add Parameterized Constructor
class Student {
    String name;
    int age;
    Student() {
        name = "Unknown";
        age = 0;
    }

    // Parameterized constructor
    Student(String n, int a) {
        name = n;
        age = a;
    }

    void display() {
        System.out.println("Name: " + name + ", Age: " + age);
    }
}

Step 5: Use Both Constructors in Main
public class Main {
    public static void main(String[] args) {

        Student s1 = new Student();          // default
        s1.display();

        Student s2 = new Student("Teju", 21); // parameterized
        s2.display();
    }
}

Step 6: Add this Keyword 
class Student {
    String name;
    int age;

    Student() {
        name = "Unknown";
        age = 0;
    }

    Student(String name, int age) {
        this.name = name;   // important
        this.age = age;
    }

    void display() {
        System.out.println("Name: " + name + ", Age: " + age);
    }
}

2. What do you mean by steap copy and shallow copy

Shallow Copy → copy reference (share same data)
You are not copying the real object, you are just copying the address (reference).
Shallow copy → sharing same house

Deep Copy → copy actual data (independent copy)
You create a new object inside constructor
Deep copy → building a new house

| Feature       | Shallow Copy   | Deep Copy     |
| ------------- | -------------- | ------------- |
| Copy type     | Reference      | Actual data   |
| Memory        | Shared         | Separate      |
| Change effect | Affects both   | Independent   |



3. static program(previous day first task)

public class StaticDemo {

    // Static variables (shared by all objects)
    static String collegeName = "Global University";
    static int studentCount = 0;

    // Instance variables (unique for each object)
    String studentName;
    int rollNo;

    // Constructor
    public StaticDemo(String name) {
        this.studentName = name;
        studentCount++;              // increase count
        this.rollNo = studentCount;  // assign roll number
    }

    // Static method
    static void changeCollege(String newName) {
        collegeName = newName;
    }

    // Instance method
    void displayInfo() {
        System.out.println("Roll No: " + rollNo +
                           " | Name: " + studentName +
                           " | College: " + collegeName);
    }

    // Main method
    public static void main(String[] args) {

        // Access static variable
        System.out.println("Initial College: " + StaticDemo.collegeName);

        // Create objects
        StaticDemo s1 = new StaticDemo("Alice");
        StaticDemo s2 = new StaticDemo("Bob");

        // Display info
        s1.displayInfo();
        s2.displayInfo();

        // Change college name
        StaticDemo.changeCollege("Tech Institute");

        System.out.println("\n--- After College Name Update ---");

        // Display again
        s1.displayInfo();
        s2.displayInfo();

        // Total students
        System.out.println("Total Students created: " + StaticDemo.studentCount);
    }
}
